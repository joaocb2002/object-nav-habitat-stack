# Defaults for objectnav.sim.SimConfig
# NOTE: Fields computed in code (e.g., rgb_height) are intentionally omitted.

# --- Simulation parameters ---
scene_dataset_config: null
scene_id: null
enable_physics: false

# --- Sensor parameters ---
obs_scale: 0.5625  # 9/16
rgb_width: 1024
sensor_height: 1.5
hfov_deg: 90.0

# --- Action space magnitudes ---
forward_step: 0.25
turn_deg: 30.0
